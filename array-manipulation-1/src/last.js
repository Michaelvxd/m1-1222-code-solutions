/* exported last */

// takes in an array
// using dot or bracket notation
// uses the .length method to find the length of the array
// subtracts one one from the length to targets the last element
// returns the last element of the array

function last(array) {
  return array[array.length - 1];
}
