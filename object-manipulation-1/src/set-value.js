/* exported setValue */

// takes in an object
// takes in a key as a string
// takes in a value (any type)
// assign the value to the object key using the assignment operator
// does not return anything
function setValue(object, key, value) {
  object[key] = value;
}
