/* exported getValue */

// takes in an object
// takes in a key as a string
// returns the value of the object found at the key using bracket notation

function getValue(object, key) {
  return object[key];
}
